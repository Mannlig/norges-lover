name: Overvåk Pi – heartbeat-sjekk

on:
  schedule:
    # Kjør kl. 08:00 UTC hver dag
    - cron: "0 8 * * *"
  workflow_dispatch:

jobs:
  sjekk-heartbeat:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      contents: read

    steps:
      - name: Hent siste commit-dato for heartbeat-filen
        id: sjekk
        uses: actions/github-script@v7
        with:
          script: |
            const { data: commits } = await github.rest.repos.listCommits({
              owner: context.repo.owner,
              repo: context.repo.repo,
              path: "data/status/heartbeat.md",
              per_page: 1,
            });

            if (commits.length === 0) {
              core.setOutput("stille", "true");
              core.setOutput("siste", "aldri");
              return;
            }

            const sisteDato = new Date(commits[0].commit.author.date);
            const now = new Date();
            const timerSiden = (now - sisteDato) / 1000 / 3600;

            core.setOutput("timer_siden", timerSiden.toFixed(1));
            core.setOutput("siste", sisteDato.toISOString());
            core.setOutput("stille", timerSiden > 6 ? "true" : "false");

      - name: Åpne issue hvis Pi er stille
        if: steps.sjekk.outputs.stille == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const timerSiden = "${{ steps.sjekk.outputs.timer_siden }}";
            const siste = "${{ steps.sjekk.outputs.siste }}";

            // Sjekk om det allerede finnes et åpent varslings-issue
            const { data: issues } = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: "open",
              labels: "pi-stille",
            });

            if (issues.length > 0) {
              console.log("Issue finnes allerede, hopper over.");
              return;
            }

            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `⚠️ Pi har ikke kjørt på ${timerSiden} timer`,
              body: [
                "## Automatisk varsel – Pi er stille",
                "",
                `Siste heartbeat-commit: **${siste}**`,
                `Stille i: **${timerSiden} timer**`,
                "",
                "### Mulige årsaker",
                "- Pi har krasjet eller mistet strøm",
                "- `norges-lover`-tjenesten har stoppet",
                "- Nettverksfeil mot GitHub",
                "",
                "### Sjekk",
                "```bash",
                "systemctl status norges-lover",
                "tail -50 logs/scraper.log",
                "pgrep -a python",
                "```",
                "",
                "*Dette issuset lukkes automatisk når Pi-en er i gang igjen og en ny heartbeat er registrert.*",
              ].join("\n"),
              labels: ["pi-stille"],
            });

      - name: Lukk issue hvis Pi er i gang igjen
        if: steps.sjekk.outputs.stille == 'false'
        uses: actions/github-script@v7
        with:
          script: |
            const { data: issues } = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: "open",
              labels: "pi-stille",
            });

            for (const issue of issues) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issue.number,
                body: "✅ Pi er i gang igjen – heartbeat registrert. Lukker automatisk.",
              });
              await github.rest.issues.update({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issue.number,
                state: "closed",
              });
            }
